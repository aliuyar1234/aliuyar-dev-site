---
import BaseLayout from "../../layouts/BaseLayout.astro";
import SiteHeader from "../../components/SiteHeader.astro";
import SiteFooter from "../../components/SiteFooter.astro";
import writing from "../../content/writing.json";

interface BlogPost {
  title: string;
  date: string;
  summary: string;
  slug: string;
  content?: string[];
  tags?: string[];
  readMinutes?: number;
}

type ContentBlock =
  | { kind: "heading"; text: string }
  | { kind: "paragraph"; text: string }
  | { kind: "list"; items: string[] };

export function getStaticPaths() {
  return [...(writing as BlogPost[])]
    .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())
    .map((post) => ({
      params: { slug: post.slug },
      props: { post }
    }));
}

const { post } = Astro.props as { post: BlogPost };
const rawContent = post.content && post.content.length > 0 ? post.content : [post.summary];
const contentBlocks: ContentBlock[] = [];
let listBuffer: string[] = [];
let paragraphBuffer: string[] = [];

const flushParagraph = () => {
  if (paragraphBuffer.length === 0) return;
  contentBlocks.push({ kind: "paragraph", text: paragraphBuffer.join(" ") });
  paragraphBuffer = [];
};

const flushList = () => {
  if (listBuffer.length === 0) return;
  contentBlocks.push({ kind: "list", items: listBuffer });
  listBuffer = [];
};

for (const rawLine of rawContent) {
  const line = rawLine.trim();
  if (!line) {
    flushParagraph();
    flushList();
    continue;
  }

  if (line.startsWith("## ")) {
    flushParagraph();
    flushList();
    contentBlocks.push({ kind: "heading", text: line.slice(3).trim() });
    continue;
  }

  if (line.startsWith("- ")) {
    flushParagraph();
    listBuffer.push(line.slice(2).trim());
    continue;
  }

  flushList();
  paragraphBuffer.push(line);
}

flushParagraph();
flushList();

const formatDate = (date: string) =>
  new Date(date).toLocaleDateString("en-US", {
    year: "numeric",
    month: "short",
    day: "numeric"
  });

const articleUrl = `https://aliuyar.dev/articles/${post.slug}`;
const copyArticleText = [post.title, "", post.summary, "", `URL: ${articleUrl}`, "", ...rawContent].join("\n");

const articleJsonLd = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  headline: post.title,
  datePublished: new Date(post.date).toISOString(),
  description: post.summary,
  url: articleUrl,
  isPartOf: {
    "@type": "Blog",
    name: "Ali Uyar Articles",
    url: "https://aliuyar.dev/articles"
  }
};
---

<BaseLayout
  title={`${post.title} — Ali Uyar`}
  canonicalPath={`/articles/${post.slug}`}
  description={post.summary}
  jsonLd={articleJsonLd}
>
  <SiteHeader />
  <main id="main" class="page-wrap py-16 md:py-24">
    <div class="flex flex-wrap items-center justify-between gap-3 border-b rule pb-5">
      <a href="/articles" class="text-link">← Back to Articles</a>
      <button
        id="copy-article-button"
        type="button"
        class="focus-ring inline-flex items-center gap-2 font-mono text-[0.72rem] uppercase tracking-[0.2em]"
        style="color: rgb(var(--muted));"
      >
        <svg class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="9" y="9" width="11" height="11" rx="1.5"/>
          <path d="M5 15V5a1 1 0 0 1 1-1h10"/>
        </svg>
        <span>Copy article</span>
      </button>
      <span id="copy-article-status" class="sr-only" aria-live="polite"></span>
    </div>
    <pre id="copy-article-source" class="hidden">{copyArticleText}</pre>

    <article class="mx-auto mt-10 max-w-[46rem]">
      <header>
        <p class="edition tabnum">
          {formatDate(post.date)}
          {post.readMinutes ? ` · ${post.readMinutes} min read` : ""}
        </p>
        <h1 class="display title-balance mt-6 text-[clamp(2.4rem,6.8vw,4.6rem)] leading-[1.04]">
          {post.title}
        </h1>
        <p class="article-summary">{post.summary}</p>

        {(post.tags || []).length > 0 && (
          <div class="mt-8 flex flex-wrap gap-2">
            {(post.tags || []).map((tag) => <span class="chip">{tag}</span>)}
          </div>
        )}
      </header>

      <div class="article-prose">
        {contentBlocks.map((block) => {
          if (block.kind === "heading") {
            return <h2>{block.text}</h2>;
          }

          if (block.kind === "list") {
            return (
              <ul class="article-list">
                {block.items.map((item) => (
                  <li>{item}</li>
                ))}
              </ul>
            );
          }

          return <p>{block.text}</p>;
        })}
      </div>

      <div class="mt-16 border-t rule pt-8 flex flex-wrap items-center justify-between gap-4">
        <a href="/articles" class="text-link">← All articles</a>
        <a href="/contact" class="text-link">Write to me →</a>
      </div>
    </article>
  </main>
  <SiteFooter />
</BaseLayout>

<script is:inline>
  (() => {
    const button = document.getElementById("copy-article-button");
    const source = document.getElementById("copy-article-source");
    const status = document.getElementById("copy-article-status");
    if (!button || !source) return;

    const label = button.querySelector("span");
    const originalLabel = (label && label.textContent?.trim()) || "Copy article";

    const setStatus = (message) => { if (status) status.textContent = message; };

    const fallbackCopy = (text) => {
      const area = document.createElement("textarea");
      area.value = text;
      area.setAttribute("readonly", "true");
      area.style.position = "fixed";
      area.style.left = "-9999px";
      document.body.appendChild(area);
      area.select();
      let ok = false;
      try { ok = document.execCommand("copy"); } catch { ok = false; }
      document.body.removeChild(area);
      return ok;
    };

    const copyToClipboard = async (text) => {
      if (navigator.clipboard && typeof navigator.clipboard.writeText === "function") {
        try { await navigator.clipboard.writeText(text); return true; }
        catch { return fallbackCopy(text); }
      }
      return fallbackCopy(text);
    };

    button.addEventListener("click", async () => {
      const text = source.textContent?.trim();
      if (!text) return;
      const ok = await copyToClipboard(text);
      if (label) label.textContent = ok ? "Copied" : "Copy failed";
      setStatus(ok ? "Article copied to clipboard" : "Failed to copy article");
      window.setTimeout(() => {
        if (label) label.textContent = originalLabel;
      }, 1800);
    });
  })();
</script>
